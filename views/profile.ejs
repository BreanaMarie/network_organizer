<!DOCTYPE html>
<html>
<head>
	<title>Profile Page</title>
  <% include ./_connections %>

</head>
<body>
<% include ./_navbar %>

<div class='container'>
  <div id='user_profile' class='row'> 
    <!--customized info box about the user-->
    <div class='col-sm-4'>
      <div id='userinfo' class="well"> 
        <h1><%= user.name %></h1>
        <p>  Here is your snap shot of your network.</p>
        </br>
        <p>You currently have <strong><%= companies.length +1 %></strong> companies that you are following right now.</p>
        <p>Take a look at all the companies you are following in your <a href="/businesses">Company Files</a>.</p>
        </br>
        <h3>Upcoming events</h3>
        <p>coming soon a list of all of your reminders</p>
      </div>
    </div>
    <!--customized headline box about user companies-->
    <div class='col-sm-8'>
      <div id="headlines" class=''>
          <h3>Companies from your hot list:</h3>
          <%for (var i=0; i < companies.length; i++) { %>
            <% if (companies[i].status == 'current'){ %>
              <% if (companies[i].hotness ==5) { %>
              <div class="col-sm-11">
                <div id='<%= companies[i]._id %>' class='well hot'>
                  <h5 class="list-group-item-heading"><%= companies[i].name %></h5>
                  <span class='list-group-item-text'><%= companies[i].city %></span></br>
                  <span class='list-group-item-text'>This company has a hotness level of:<%= companies[i].hotness %></span></br>
                   <button data-id='<%= companies[i]._id %>' class="view_co"> View Profile</button> 
                </div>
              </div>
              <% } %>
            <% } %>
          <% } %>
      </div>
    </div>
  </div>
<!--Link to user's filed companies-->  
  <div id='usercontent'>  
    <h1><a href="/businesses">Company Files</a></h1>
    <p>Take a look at all the companies you are following.  You can see more details about each company by clicking the 'View Profile" button or you can remove the company from your profile completely by clicking the delete button. Go to <a href="/businesses">Company Files</a></p>
  </div>
</div>
</body>
<% include ./_footer %>
</html>